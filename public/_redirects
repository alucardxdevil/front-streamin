# Cloudflare Pages — Redirects
# Los archivos estáticos en public/ (robots.txt, logo-icon.png, etc.)
# se sirven automáticamente ANTES de estas reglas.

# Perfiles legacy → formato universal @slug (301 para SEO)
/profileUser/:slug  /@:slug  301

# SPA catch-all: Todas las rutas no-estáticas sirven index.html
# Nota: El Cloudflare Pages Function (functions/[[path]].js) se ejecuta
# ANTES de estas reglas y maneja crawlers + 404 dinámicos.
/*    /index.html   200
